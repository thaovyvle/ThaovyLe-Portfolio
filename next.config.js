/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: '/ThaovyLePortfolio',
  images: {
    loader: "default",
    path: "/_next/image",
  },
};

module.exports = nextConfig;
