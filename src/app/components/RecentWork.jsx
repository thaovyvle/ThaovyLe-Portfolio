"use client";
import React, { useState, useRef } from "react";
import { useMediaQuery } from 'react-responsive';

const RecentWork = () => {

    return (
        <section id="recentWork">
            <h1 className="text-center text-4xl lg:text-5xl md:text-5xl font-semibold tracking-[0.2em] mb-8 md:mb-12 mt-[2.5em] bg-gradient-to-r from-primary-400 to-secondary-600 text-transparent bg-clip-text">
            MOST RECENT WORK
            </h1>
        </section>
    );
}

export default RecentWork;